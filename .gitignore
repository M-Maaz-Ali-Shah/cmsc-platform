node_modules
.next
.env*
!.env.example
.dev.vars
!.dev.vars.example
.open-next
.wrangler
*.tsbuildinfo
.DS_Store

# Playwright — contains a real session JWT for the test admin, and
# run artifacts that are regenerated on every run.
/tests/.auth
/playwright-report
/test-results
/blob-report
